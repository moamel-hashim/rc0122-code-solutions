/* exported getLastChar */

/* first we create a function that holds a string as a parameter
then we return the last character of that string */

function getLastChar(string) {
  return string[string.length - 1];
}
