/* exported isLowerCased */

/* create a function that checks if the word is lower cased or not and the return value should be a boolean
*/

function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  } else {
    return false;
  }
}
