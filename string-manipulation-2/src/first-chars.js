/* exported firstChars */

/* create a function that takes 2 parameters
function firstChars(length, string) {
  create a variable that holds the value of the string after using the method substr on it
  const myString = string.substr(0, length);
  return myString
}
*/

function firstChars(length, string) {
  const myString = string.substr(0, length);
  return myString;
}
