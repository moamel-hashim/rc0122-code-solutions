/* exported getFirstChar */

/* first we need to make a function that holds a string as a parameter
then we return the first index of that string
*/

function getFirstChar(string) {
  return string[0];
}
